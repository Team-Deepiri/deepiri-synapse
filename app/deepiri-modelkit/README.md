# deepiri_modelkit — event models

This package contains small Pydantic event models used for validating runtime
events in the platform. The module of interest is
`deepiri_modelkit.contracts.events`.